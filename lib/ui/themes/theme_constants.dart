import 'package:flutter/material.dart';
const Radius CIRCULAR_RADIUS = Radius.circular(7.0);
const BorderRadius BORDER_RADIUS = BorderRadius.all(CIRCULAR_RADIUS);
const ShapeBorder BORDER_SHAPE = RoundedRectangleBorder(borderRadius:BORDER_RADIUS);
final OutlineInputBorder INPUT_BORDER = new OutlineInputBorder(borderRadius: BORDER_RADIUS);
