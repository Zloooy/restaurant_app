import 'package:flutter/material.dart';
const Radius CIRCULAR_RADIUS = Radius.circular(7.0);
const BorderRadius BORDER_RADIUS = BorderRadius.all(CIRCULAR_RADIUS);
const ShapeBorder BORDER_SHAPE = RoundedRectangleBorder(borderRadius:BORDER_RADIUS);
const OutlineInputBorder INPUT_BORDER = const OutlineInputBorder(borderRadius: BORDER_RADIUS);
const EdgeInsets LIST_SEPARATOR_PADDING = EdgeInsets.only(bottom:25);
const double ELEVATION = 5;
const double DEFAULT_SPACING = 20.0;
